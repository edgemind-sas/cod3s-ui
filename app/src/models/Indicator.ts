export default class Indicator {
  observer!: string;
  type!: string;
  measure!: string;
  stats!: Array<string>;
  value!: string | boolean;
  description!: string;
  name!: string;
  unit!: string;
  block!: string;
  id!: string;
}
